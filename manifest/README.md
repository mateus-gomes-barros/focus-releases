# Stable update manifest

The production manifest will be stored at:

`manifest/stable.json`

It is intentionally published only after the corresponding signed APK is fully available and independently verified. A missing manifest means that no self-update release has been activated yet.

Internal, beta, and unreleased manifests are not stored in this public repository.
